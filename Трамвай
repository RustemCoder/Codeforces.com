#include <iostream>

int main()

{
	
	int n;
	std::cin>>n;
	
int a[n],b[n];
int max=0;
int sum=0;
for(int i=0;i<n;i++)
{
	std::cin>>a[i]>>b[i];
	
}
	
	
	for(int i=0;i<n;i++)
	{
		sum =sum+b[i];
		sum =sum-a[i];
		if(sum>max)
		{
			max=sum;
		}
	}
	
	std::cout<<max;
	return 0;
}
